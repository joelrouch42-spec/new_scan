{
  "smc": {
    "order_blocks": {
      "enabled": true,
      "impulse_threshold": 1.0,
      "min_body_percent": 0.15,
      "description": "impulse_threshold: ratio du body de la bougie d'impulsion vs moyenne (1.0 = 1.0x la moyenne). min_body_percent: body minimum en % du range total (0.15 = 15%)"
    }
  }
}
